spring:
  profiles:
    active: vm
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springdemo
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update